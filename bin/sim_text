#!/bin/bash

if [ -z "$1" ]; then
	echo "USAGE: sim_text <trace.txt> [ -s source.mdl ] [ -t target.mdl ] " >&2
  exit 0
fi
in="$1"
shift
trace_binarize /dev/stdout < "$in" | sim -a /dev/stdin "$@" --logtostderr
